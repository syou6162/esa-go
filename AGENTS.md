# AGENTS.md

コーディングエージェント向けの作業指示です。

## PR を出す前に必ず実行すること

以下の 4 コマンドをすべて通してください。

```bash
gofmt -l .
go vet ./...
go test ./...
go build ./...
```

- `gofmt -l .` の出力は空であること
- 各コマンドがエラーなく終了すること

## PR 作成時

- PR 本文は [`.github/PULL_REQUEST_TEMPLATE.md`](.github/PULL_REQUEST_TEMPLATE.md) に従って記入すること
- 実際に実行・確認した項目だけチェックボックスをオンにすること
- PR 作成後、CI が成功していることを確認すること

## 設計制約

このライブラリは複数アプリから共通利用される public module です。以下を守ってください。

1. **アプリ固有の固定値を埋め込まない**
   - team 名、カテゴリ prefix、投稿名、許可タグなどは、ライブラリの公開 API やデフォルト値に含めない
   - 呼び出し側が引数・オプションで渡す

2. **アプリ SDK / config に依存しない**
   - MCP SDK、Slack SDK、アプリの config 読み込みに依存しない
   - 標準ライブラリと esa.io API に必要な最小限の依存のみとする

3. **アプリ側の並行制御を持ち込まない**
   - `KeyedMutex` による日付単位の直列化など、アプリ固有の並行制御はライブラリに含めない

4. **secret を漏らさない**
   - access token などの secret を値としてコード・ログ・テスト・PR・コミットに含めない
   - エラーメッセージに URL やトークンを載せないよう redaction に注意する
